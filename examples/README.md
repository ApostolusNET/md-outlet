# examples

Bundled fixtures for smoke tests and the UI **ガイド → サンプル文書**.

| Keep | Role |
|------|------|
| `sample.*` | Minimal demos (md / xml / json / yaml / txt / log / csv …) |
| `sample-frontmatter.md` | Front matter demo |
| `fixtures/` | Tiny assets for automated tests |

Personal extras go in `local/` (gitignored).
